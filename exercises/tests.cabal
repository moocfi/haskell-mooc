name: tests
version: 0.0.1
build-type: Simple
cabal-version: >= 1.10

library
  build-depends: QuickCheck, hspec, base, containers, array, transformers, template-haskell, generic-random, JuicyPixels, HTTP, text, bytestring, wai, warp, sqlite-simple, hedgehog
  default-language: Haskell2010
